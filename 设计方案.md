# 英语悄悄学（stealth-english）· 功能清单与设计实现方案

> 更新：2026-09-16。M1~M5 均已落地，本文档描述的是**当前代码的实际状态**，功能有增删直接改这里。
> M5 已从 Electron 切到 Tauri：`electron/`、`core/store.js`、`assets/` 与 Electron 版的打包脚本一并删除，不再保留回退路径。
> 注意 `tools/pack.js` 是**后来为 Tauri 版重写的同名新文件**，和上面删掉的那个无关，看到同名别以为是残留。
> `design/mockup.html` 是最初的静态外观原型，已定型并**停止维护**（里面还有 EPUB、粘贴导入这些后来砍掉的按钮）。要看真实界面：`npm run dev`，代码在 `src/`。
> `design/` 只放这类历史设计资料（原型 + 音色样例），整个目录已被 `.gitignore` 排除，不进仓库、不参与构建。

---

## 0. 常用命令

| 目的 | 命令 | 说明 |
|---|---|---|
| 开发运行 | `npm run dev` | 即 `tauri dev`，会自动先跑 `npm run web` |
| 只重新生成前端 | `npm run web` | `web/` 是生成物，改完 `src/` 必须跑 |
| 逻辑自测 | `npm test` | 跑 `core/test.js`，27 项检查 |
| 打包便携版 | `npm run pack` | → `dist/stealth-english/`，加 `--skip-build` 跳过 cargo 编译 |
| 重新生成图标 | `npm run icon` | 写入 `src-tauri/icons/` |
| 其他 tauri 命令 | `npm run tauri -- <args>` | 例如 `npm run tauri -- build` |

要点：

- 页面源码在 `src/`，Tauri 实际加载的是生成出来的 `web/`（把 CommonJS 的 `core/` 包装成浏览器能跑的形式 + 注入 IPC 桥）。**改了 `src/` 或 `bridge/` 不跑 `npm run web`，界面不会变。**
- 数据目录：开发态是项目根的 `data/`，发布态是 exe 旁边的 `data/`，两边格式完全一样。
- 前置环境：Rust 工具链（cargo）+ Windows WebView2（Win10/11 一般自带）。

---

## 1. 技术选型

### 结论：Tauri 2（Rust）+ 原生 HTML/CSS/JS

| 层 | 技术 | 说明 |
|---|---|---|
| 界面 | HTML / CSS / JavaScript | 悬浮窗与资料库各一个窗口，两个页面互不干扰 |
| 外壳 | Tauri 2（Rust） | 系统 WebView2 渲染，产物几 MB |
| 数据 | 本地 JSON（Rust 侧读写） | 不联网、不装数据库，资料全在自己电脑上 |
| 语音 | Windows 系统语音（离线 `speechSynthesis`） | 见第 4 节 |
| 打包 | `tools/pack.js` → 便携版**文件夹** | 免安装免管理员权限，整个文件夹拷走就能跑 |

**为什么选 Tauri：**

1. **小**：exe 几 MB，内存占用远低于 Electron（约 150~250MB），办公机上更干净。
2. **外观零损失**：悬浮窗的三种伪装模式、透明度渐变这些效果本来就是网页做的；窗口级的无边框透明、置顶、不出现在任务栏和 Alt+Tab 由 Rust + Win32 直接实现，比 Electron 更可控。
3. **隐身所需能力齐全**：托盘、全局快捷键、单实例、开机自启都有官方插件（`tauri-plugin-*`）。

**代价：** 需要 Rust 工具链，`cargo build` 比改完刷新慢一些（首次编译几分钟，之后增量快）。

**历史：** M1~M4 是 Electron 实现的，M5 整体迁到 Tauri，`src/` 下的界面代码原样复用——页面只依赖 `window.api` 这一层接口，后端换成 Rust 后前端无感。

---

## 2. 功能清单

### A. 悬浮学习窗（核心）

- 无边框、透明背景、始终置顶、**不出现在任务栏 / Alt+Tab**，默认右下角
- 可拖动，位置、大小、透明度、字号、字体颜色、模式、伪装标题全部记住（下次启动还原）
- 三种伪装模式：通知卡片 / 纯文字 / 字幕条
- 通知卡片的标题**直接在悬浮窗上点着改**（`contenteditable`，失焦即存），默认「工作提醒」
- 尺寸两种：`按句子长度自适应`（受「最大宽度」260~900 约束）/ `固定`（拖右下角手柄定尺寸）
- **字体颜色可自定义**（默认纯白）。桌面或所在软件是浅色时可调深，程序按颜色亮度自动配一圈对比描边，深浅都看得清
- 中文翻译三种策略：总是显示 / 悬停显示 / 隐藏（纯听感）
- 鼠标移开 3 秒自动变淡，移回恢复（可关）
- 当前句、书名、进度（第 x / 共 y 句）
- 隐藏时自动停止朗读（自动播放的下一句链也随之中断）
- 拖动：WebView2 不认 CSS `-webkit-app-region`，改由页面 mousedown 调 `widget_drag`，Rust 侧起拖

### B. 练习

- 上一句 / 下一句 / 重播当前句 / 播放暂停
- 顺序播放 / 随机播放 / 单句循环
- 自动播放：每句朗读完后按设定间隔（3~60 秒）切下一句，挂机磨耳朵
- 练习单位（导入时定）：单句 / 智能合并（≤30 词一卡）/ 整段
- **资料库「练习」页是一页可滚动的句子列表**：进来就能看到整页句子，当前句高亮并自动滚到视野内，点任意一句就从那里开始练
- **列表分页**：每页 20 / 50 / 100 句，默认 20，选择会记住。几千句的书也只渲染当前这一页（2000 句整本渲染 469ms → 分页后 5ms）
- **回忆测试**：勾「遮住英文」→ 列表里的英文变模糊，点一下揭晓该句

### C. 老板键与全局快捷键（说明见 4.3）

- `Alt+H` 显示 / 隐藏悬浮窗，`Alt+←` / `Alt+→` 切句，`Alt+P` 播放 / 暂停
- 全局生效（`tauri-plugin-global-shortcut`），四个键都能在设置页改：点输入框直接按组合键即可录入
- 注册后这些组合键被本程序独占，所以要求必须带 Alt / Ctrl
- 注册失败不阻塞启动，只弹一次警告，可在设置里换

### D. 导入与资料库

- 导入双语 **TXT**（规则详见第 3 节）
- 自动识别编码（UTF-8 / GBK / GB18030 / UTF-16——中文 txt 常是 GBK，必须处理），解码在 Rust 侧（`textfile.rs`）
- 导入前先**预览**：识别到的对齐方式、分句结果、句数统计 + 样例，确认后才入库。**不会未经预览直接写库**
- 课/章自动识别：`Lesson 3`、`第 3 课`、`Chapter 3` 等行作为分节边界，逐节探测对齐规则（只影响解析，不在界面上分组展示）
- 资料库：多本书并存、各记各的进度、封面色块、书名可改、可删除

### E. 语音朗读

- **神经语音（默认）**：微软 Edge 同款云端引擎，内置 6 个英文音色（小女孩 / 女人 / 男人 各 2 个，全为原音色不变调），首次朗读需联网，之后走本地缓存（详见 4.2）
- **系统语音（兜底）**：关掉神经语音、断网或接口失效时自动用 Windows 本地语音朗读；**不提供音色选择**（自动取第一个 `lang` 以 `en` / `zh` 开头的系统语音，机器上没有就交给浏览器默认）
- 中文朗读可选开关（听一遍英文 → 再听一遍中文，中文也走神经语音 `晓晓`）
- 语速 0.5x~1.5x，练习页顶部也有语速滑杆，两边同步
- 详见第 4 节

### F. 托盘与窗口行为

- 托盘左键：显示 / 隐藏悬浮窗
- 托盘右键菜单：显示/隐藏悬浮窗、打开资料库、**开机自启**（勾选式）、退出
- 关闭资料库窗口 = 隐藏到托盘，不退出程序
- 单实例：重复双击 exe 不会开第二个，而是把资料库叫到前面
- 首次运行自动灌入内置示例书（自编 288 句，按由浅入深的语法线排），避免书库是空的

### G. 设置页（六组）

- 悬浮窗外观：伪装模式、不透明度 15~100、字号 11~22、字体颜色、最大宽度、窗口尺寸、中文翻译、移开变淡
- 资料库窗口：整体不透明度 35~100（含标题栏，一键调淡到"不像个窗口"）
- 播放：自动播放、切换间隔、单句循环、随机顺序
- 语音：神经语音开关、英文音色（6 个，按 3 类分组）、缓存大小与清理、试听、朗读中文、语速
- 查词：词典缓存大小与清除
- 全局快捷键：四个组合键
- 当前学的是 Which 书不需要设——程序自动记住上次打开的书

### H. 点词查义

- 悬浮窗里的英文单词可点击，点一下出词卡：**单词 + 英/美音标 + 词性 + 中文释义**（刻意不带例句）
- 词卡出现在**原文上方**，窗口底边钉住、向上生长，原文位置一动不动（不遮挡、不推移正在读的句子）
- 查过的词缓存在本地 `data/cache/dict.json`，再点同一个词秒出，断网也能看
- **只有失败态才显示文字提示**（原因 + 「重试」「复制单词」两个按钮），成功态不显示任何来源或耗时
- 卡片右上角常驻 `✕` 关闭；再点同一个词 / 点句子空白处 / 按 Esc / 切换句子，也都会收起词卡
- 详见第 8 节

### 还没做的（原 v1.x / v2 想法，按需要挑着做）

- 学习统计：今日句数、连续天数、每本书累计
- 回忆测试补全：遮住中文、悬浮窗里也能回忆
- 生词本：把查过的词收藏起来（**查词本身已做，见 H 节**；词典缓存表天然就是"查过的词"列表，接生词本成本很低）
- 在线神经语音 + 边听边缓存（音质大幅提升，见 4.2）
- 机器翻译：对只有英文的书补中文（需联网，可选）
- 入库后改练习单位（目前只能重新导入）
- （v2）跟读录音与回放对比、双语 SRT 导入、更多伪装皮肤（CPU 监控 / 股票 / 便签）、多显示器记忆、贴边吸附

---

## 3. 导入规则（重点设计）

### 3.1 支持的输入

| 来源 | 说明 |
|---|---|
| 双语 TXT | 唯一入口。编码自动识别（UTF-8 / GBK / GB18030 / UTF-16） |

> EPUB、直接粘贴文本在原型里画过，实际没做——TXT 已能覆盖双语对照这类资料，EPUB 手动转 txt 即可。

### 3.2 中英对齐规则（自动识别，预览前可手动改）

导入的第一步是搞清楚「哪段英文配哪段中文」。`core/align.js` 实现四条规则，`自动识别`会全试一遍；导入页的手动兜底只开放最常用的 **A** 和 **D** 两条（B / C 自动识别能命中，暂不做手动选项）。

**规则 A：逐段交替**（最常见，双语 txt 多是这种）
```
Once upon a time, there was a little girl called Alice.
从前，有一个名叫爱丽丝的小女孩。
She was sitting under a tree with her sister.
她正和姐姐坐在树下。
```
判定：段落按「英文段 ↔ 中文段」交替（相邻段落语种不同的比例足够高）。配对：第 2n+1 段 ↔ 第 2n+2 段。语种按一段里汉字占比判定。

**规则 B：两段式 / 块状**（前面整篇英文、后面整篇中文，或反过来）
```
（英文段落 × N）
（中文段落 × M）
```
判定：整个序列语种**至多切换一次**（`en…en cn…cn`）→ 命中 B，且 B 优先于 A（“一段课文 + 一段译文内含多段”就是这种）。配对：按段落序号 i ↔ i，数量不等时多出的部分留空并在预览页给出警告。B 也是识别不了时的兜底规则。

**规则 C：行内对照**（一行里中英都有）
```
Hello world!	你好，世界！
Long time no see.｜好久不见。
```
判定：一半以上的行同时含英文和汉字。分隔符按优先级尝试：`Tab` → `｜` → `|` → `//` → ` # `。（不支持手动指定分隔符。）

**规则 D：仅英文**
全文基本无汉字 → 只导入英文，中文留空，卡片上不显示译文。补齐中文（段落对照 / 机器翻译）在未做清单里。

**识别结果怎么确认**：预览页给出统计（英文段 x / 中文段 y / 共 N 张练习卡）、各规则命中数、警告列表和前 12 句样例，看着不对就切「A 逐段交替」或「D 仅英文」重新生成预览，确认后才入库。

### 3.3 自动分句规则

对每段英文文本：

1. **并回断行**：段内硬换行先接回空格（txt 常见排版断行）。
2. **保护缩写**：`Mr. Mrs. Ms. Dr. Prof. St. vs. etc. e.g. i.e. U.S. U.K.` 及人名首字母缩写（`J. K. Rowling`）里的点不切断。
3. **保护数字与省略号**：`3.14`、`...`（三个点算一个省略号）不切断。
4. **切分**：在 `. ! ? … ?!` + 后引号/括号之后、后跟空格+大写字母（或行尾）处切。
5. **合并碎片**：切出来少于 2 个单词或 8 个字符的碎片并回前一句。

**中文侧的配对问题（诚实说明限制）**：中文翻译通常不与英文逐句对应（一句话英文可能译成两句中文）。策略：
- 把中文段也按 `。！？；` 切句；
- 若段内中英句数相等 → 逐句 1:1 配对（规范的双语对照大多如此）；
- 若数量不等 → 该段的**整段中文**附到这段里的每一张英文卡上（宁多勿错）；
- 也可以直接选「整段模式」：一张卡 = 一段英文 + 一段中文，最稳。

### 3.4 练习单位（导入时选，入库后不可改）

- **单句**：一句一卡（默认）
- **智能合并**：相邻短句合并，每卡不超过约 30 词，听感更自然
- **整段**：一段一卡（中文对齐最可靠）

想换单位只能重新导一次（几秒钟的事）。

### 3.5 入库后的数据

每本书一个文件夹，纯 JSON，人肉可读可改：

```
data/                       ← 开发时在项目里，打包后在 exe 旁边
  config.json               ← 全局设置（快捷键、外观、声音、进度…）
  books/
    b_m1q2z3_ab12/          ← b_ + 时间戳(base36) + _ + 4 位随机
      meta.json             ← 书名、来源、命中的对齐规则、练习单位、句数、progress.index
      sentences.json        ← [{ "en":"...", "cn":"..." }, ...]
  logs/app.log              ← Tauri 侧的运行日志
```

进度：meta 很小，直接写盘（先写 `.tmp` 再改名，避免写一半被强杀留下坏文件）。

---

## 4. 语音功能设计

### 4.1 兜底方案（离线）：Windows 系统语音

默认走 4.2 的神经语音；这里是**断网 / 接口失效 / 用户主动关闭**时的离线兜底。
- 声音选择：**没有选择**（设置页那两个下拉框已于 2026-09-20 移除），取第一个 `lang` 以 `en` / `zh` 开头的系统语音，一个都没有就交给浏览器默认。
- 支持语速（0.5x~1.5x）、暂停、停止。
- 局限：这些是老合成器（David / Zira / Mark），机械感重。

**关键事实：Chromium（含 WebView2）只认 OneCore 语音，不认 SAPI5。**

Windows 有两套互不相通的语音注册表：

| 注册表位置 | 谁在用 | 典型条目 |
|---|---|---|
| `HKLM\SOFTWARE\Microsoft\Speech\Voices\Tokens`（SAPI5） | 老桌面程序、讲述人 legacy | `TTS_MS_EN-US_ZIRA_11.0` |
| `HKLM\SOFTWARE\Microsoft\Speech_OneCore\Voices\Tokens` | 现代 API、Edge 朗读、讲述人、**Chromium / WebView2** | `MSTTS_V110_enUS_DavidM` |

所以「下拉框里有没有英文声音」取决于 **OneCore 里有几个英文语音**，跟控制面板里看到的 SAPI 列表无关。机器只装了中文系统时，OneCore 里往往一个英文都没有 → 下拉框只有一个「自动」，英文实际由系统默认声音朗读，听感最差。

用户自助加语音（无需改代码）：设置 → 时间和语言 → 语音 → 管理语音 → 添加语音 → English (United States)。装完**重启本程序**（`getVoices()` 在进程启动时取）。

想让已有的 SAPI 声音被 WebView2 看到，可用管理员 PowerShell 把 TokenEnums 挂到 OneCore 下：

```powershell
Copy-Item HKLM:\SOFTWARE\Microsoft\Speech\Voices\TokenEnums\ HKLM:\SOFTWARE\Microsoft\Speech_OneCore\Voices\ -Recurse
```

### 4.2 增强方案：神经语音（Edge 免费端点 / Azure）+ 本地缓存

系统语音听感不够，改走云端神经语音：**按句合成 → 缓存成本地 mp3 → 之后离线秒播**。
请求放 Rust 侧（`reqwest`）而不是页面 `fetch`，避免 WebView2 里的跨域限制。

**Edge 免费端点实测（2026-09-17，本机可直连）：**

- `GET https://speech.platform.bing.com/consumer/speech/synthesize/readaloud/voices/list?trustedclienttoken=6A5AA1D4EAFF4E9FB37E23D68491D6F4`
  → 共 **322 个音色**，其中 `en-US` 16 个：`Ava / Andrew / Emma / Brian / Aria / Jenny / Guy / Ana / Christopher / Eric / Michelle / Roger / Steffan` 及其 Multilingual 变体。
- 新一代（Ava、Andrew、Emma、Brian）比老的 Aria / Jenny / Guy 更自然，优先试这几个。
- **没有 DragonHD**（`*DragonHD*` 查询为空）——那是 Azure 付费层专属。
- 代价：接口未公开、无 SLA，微软改过几次鉴权（`Sec-MS-GEC`），可能哪天失效；商用条款是灰色地带。所以代码里必须留「失败自动回退系统语音」。

**备选：Azure AI Speech（官方，稳定）**

- F0 免费层：**每月 50 万字符，永久免费**（按每句 80 字符算 ≈ 6000 句/月，配合缓存基本用不完）。国内用 azure.cn 同样 50 万/月。
- 标准神经语音 $16/百万字符；Neural HD（DragonHD，最顶级）$22/百万字符。HD 音色如 `en-US-Ava:DragonHDLatestNeural`、`en-US-Andrew:DragonHDLatestNeural`，支持情绪/风格标签。
- 需要 key + Azure 资源（注册门槛），音质与 Edge 同源但更稳、可选 HD。

**本地神经模型（Kokoro-82M，Apache-2.0）**：ONNX 版 fp32 310MB / fp16 163MB / int8 86MB，CPU 上约 0.5~1.5x 实时，英文质量接近商用；但要往 Tauri 里塞 onnxruntime + espeak-ng，工程量大，仅当必须完全离线时考虑。

**缓存体积估算**（默认 `audio-24khz-48kbitrate-mono-mp3` = 6 KB/秒；一句英文约 5~7 秒 ≈ 35 KB）：

| 已朗读句数 | 48kbps（推荐） | 96kbps |
|---|---|---|
| 500 句 | ~18 MB | ~36 MB |
| 2000 句 | ~70 MB | ~140 MB |
| 5000 句 | ~175 MB | ~350 MB |

缓存按「文本 + 音色 + 语速」哈希命名，同一句只存一份，所以**上界 = 句数 × 35KB**，会收敛而不是无限涨。仍应加一个上限（如 500MB）与「清理缓存」按钮，按最久未用淘汰。

**实现状态：已实现**（`src-tauri/src/tts.rs`）。要点：

- 协议：WebSocket `wss://speech.platform.bing.com/consumer/speech/synthesize/readaloud/edge/v1`，
  鉴权 `Sec-MS-GEC` = SHA256(Windows 文件时间取整到 5 分钟 + TrustedClientToken)。
  握手**必须**带 `Origin: chrome-extension://...` 和 Edge 的 `User-Agent`，否则 403（实测）。
  发 `speech.config` → `ssml` 两帧，收二进制帧（`Path:audio\r\n` 之后才是 mp3）直到 `Path:turn.end`。
- 合成结果 → base64 交给页面，转成 **Blob URL** 给 `<audio>`（不走 asset 协议，省掉安全配置）。
- **CSP 必须放行音频**：`tauri.conf.json` 里 `media-src 'self' data: blob:`。只写 `default-src 'self'` 时 `<audio src="blob:...">` 会被拦掉，表现就是"点了没声音"。
- **切片要跳过协议头**：音频帧是 `Path:audio\r\n` + mp3，从标记处切会把文本混进 mp3 开头，浏览器解码不出来。落盘前再校验一次 mp3 头（`ID3` 或 `0xFFEx` 同步字），不合格就当失败，绝不能写进缓存——坏缓存会被一直复用。
- 失败一律回退系统语音：没网、接口变了、超时（25 秒看门狗）都不打断自动播放链。资料库页失败时会 toast 出具体原因，避免"静默无声"没法排查。
- 设置页：神经语音开关 + 音色下拉 + 缓存大小/清理 + 试听；关掉开关就完全离线。

**内置 6 个音色**（3 类 × 2，清单由 Rust 下发，页面不写死；**一律用原音色，不做 pitch 变调**）：

| 类别 | 预设 id | 音色 | 备注 |
|---|---|---|---|
| 小女孩 | `girl-ana` | en-US-AnaNeural | 美音童声，软萌（微软官方标注 Child） |
| 小女孩 | `girl-maisie` | en-GB-MaisieNeural | 英音童声，清亮（官方标注 Child，F0 301Hz） |
| 女人 | `woman-ava` | en-US-AvaNeural | 自然口语（默认） |
| 女人 | `woman-aria` | en-US-AriaNeural | 清晰朗读腔 |
| 男人 | `man-andrew` | en-US-AndrewNeural | 温暖自然 |
| 男人 | `man-chris` | en-US-ChristopherNeural | 新闻播报 |

中文朗读用 `zh-CN-XiaoxiaoNeural`（同一个端点）。
**没有"小男孩"这一类**：Edge 免费列表里英文真童声只有上面两个女童声，男孩童声在 Azure 付费层；变调做出来的假童声听感更差，所以不做。

音质路线的代价对比（决定要不要往代码里塞 TTS）：

| 路线 | 体积 | 联网 | 工作量 | 音质 |
|---|---|---|---|---|
| 系统 OneCore 语音（现状） | 0 | 否 | 0 | 机械 |
| `msedge-tts` 神经语音 | JS 库约 100KB，音频按句缓存 | 首次需要 | 中：HTTP + 缓存 + 播放 + 回退 | 接近真人 |
| 离线神经（piper / sherpa-onnx） | 模型 20~60MB + onnxruntime | 否 | 大：native 集成进 Tauri | 接近真人 |

结论：先让用户装系统语音，真不够再加 `msedge-tts`；离线神经方案体积和集成成本都不划算。

### 4.3 朗读流程与快捷键的一个重要修正

朗读流程（自动播放模式）：
```
朗读英文 →（可选）朗读中文 → 停顿 N 秒 → 下一句
```

**老板键从原型里的单个 `H` 改为 `Alt+H`。**
原因：全局快捷键要求带修饰键——如果注册单个 H，你在任何软件里打字（微信回消息、写文档）按到 H 都会触发隐藏，完全没法用。所以正式版：
- `Alt+H` 显示/隐藏，`Alt+←/→` 切句，`Alt+P` 播放/暂停，全部可在设置里改。
- 悬浮窗自身获得焦点时（鼠标点过它），单键 H / ← / → 仍然有效，保留原型的手感。

### 4.4 隐私

- 所有语音在本地合成，不上传任何学习数据；程序不联网也能完整使用。

---

## 5. 项目结构

```
learn-english/
  设计方案.md             ← 本文档
  design/                 ← 历史设计资料（整个目录被 .gitignore 排除，不进仓库）
    mockup.html           ←   历史外观原型，已定型不再维护（含已砍掉的按钮，别当需求看）
    mockup-dict.html      ←   历史查词卡原型，功能已落地
    voice-samples/        ←   早期 TTS 音色试听样本
  AGENTS.md               ← 给 AI 编码代理看的动手须知
  package.json            ← scripts: dev / web / test / icon / pack
  src/                    ← 页面源码（改这里）
    widget/               ← 悬浮窗（index.html + widget.js）
    manager/              ← 资料库窗口：资料库 / 导入 / 练习 / 设置（index.html + manager.js）
  core/                   ← 纯逻辑，界面无关，node 下可直接测
    align.js              ← 语种判定、课/章切分、对齐规则 A/B/C/D 与自动探测
    split.js              ← 分句、碎片合并、智能合并、词数统计
    importer.js           ← 编码识别（readText）+ 导入流水线（parse / preview / buildBook）
    test.js               ← 自测：npm test
  bridge/
    api.js                ← window.api：页面与 Rust 之间唯一的接口层
  web/                    ← 生成物！npm run web 生成，Tauri 加载它，别手改
  src-tauri/              ← Rust 后端
    icons/                ← 应用图标（npm run icon：画源图后交给 tauri icon，只保留 32x32 / 128x128 / icon.ico）
    src/
      lib.rs              ← 装配入口：插件、托盘、快捷键
      windows.rs          ← 悬浮窗 / 资料库窗口、拖动、显示隐藏
      state.rs            ← 书库状态、切句、下发负载
      store.rs            ← data/ 下的 JSON 读写
      hotkeys.rs          ← 全局快捷键注册
      tts.rs              ← 神经语音：Edge 端点 + Sec-MS-GEC 鉴权 + mp3 缓存
      dict.rs             ← 点词查义：有道 jsonapi + 字段校验 + 裁剪 + 词典缓存
      textfile.rs         ← 选文件 + 编码识别解码
    tauri.conf.json       ← frontendDist 指向 ../web
  tools/
    sync-web.js           ← 生成 web/：复制 src/ + 包装 core/ + 注入 bridge
    pack.js         ← 便携版打包：npm run pack
    gen-icon.js           ← 生成图标：npm run icon
  data/                   ← 运行时数据（书库 + 配置 + 日志）
  dist/stealth-english/  ← 打包产物，可直接拷走运行
```

分工原则：**只有 Rust 能做的放 Rust**（窗口、托盘、快捷键、读文件与编码、写盘），**其余全在网页里**。导入的切分与对齐规则只有一份 JS 实现（`core/`），node 里能测、浏览器里能跑，靠 `sync-web.js` 包一层。

编码识别没有单独的文件——Rust 侧在 `textfile.rs`，预览阶段若走 JS 路径则是 `importer.js` 的 `readText()`：先看 BOM（UTF-16LE/BE、UTF-8 BOM），否则严格 UTF-8 解码，失败再按 GB18030 兜；含 NUL 或乱码字符过多直接报错「请另存为 UTF-8 后再导入」。

---

## 6. 开发里程碑

| 里程碑 | 内容 | 状态 |
|---|---|---|
| **M1** | 桌面壳 + 悬浮窗（透明/置顶/无任务栏/拖动/记忆位置）+ 句子练习 + `Alt+H`/`Alt+←→` + 托盘 + 内置示例书 | ✅ |
| **M2** | 导入流水线（TXT 全规则 + 编码识别 + 预览页）+ 资料库窗口 | ✅ |
| **M3** | 语音（系统声音 + 语速 + 中文朗读 + 自动播放）+ 设置页 | ✅ |
| **M4** | 便携版打包 + 程序图标 + 开机自启 | ✅ |
| **M5** | 从 Electron 迁到 Tauri（Rust 后端 + `bridge/api.js` 接口层），删掉 Electron 全部代码与产物 | ✅ |
| 之后 | 分页练习列表、字体颜色可调、资料库窗口不透明度 | ✅ |
| **M6** | 点词查义：悬浮窗点单词出词卡（有道 jsonapi + 本地词典缓存 + 设置页可清除），Rust 侧 16 项单测 + 1 项联网端到端 | ✅ |

---

## 7. 风险与对策

| 风险 | 对策 |
|---|---|
| 公司电脑可能限制安装软件 | 交付**便携版文件夹**（`dist/stealth-english/`），免安装免管理员权限；被杀软误报时加白名单 |
| 机器没有 WebView2 | Win10/11 一般自带；真没有时引导装 WebView2 Runtime（离线安装包） |
| 双语文件格式千奇百怪 | 一切导入先预览再入库；对齐规则可手动切换；错不了的才写库 |
| 中文 txt 是 GBK 编码乱码 | 编码自动识别（UTF-8 / GBK / GB18030 / UTF-16）。识别不了时不猜，直接报错提示另存为 UTF-8，界面里**不能**手动指定编码 |
| 依赖 Rust 工具链 | 只出包和改后端时需要。**前端资源是编译期嵌进二进制的**（`tauri.conf.json` 没设 `devUrl`，`tauri-codegen` 走 `EmbeddedAssets`），所以改完 `src/` 要 `npm run web` 再重跑 `npm run dev`——tauri-codegen 监视 `web/`，会自动触发重编，不用手动 `cargo build`；打包版则必须重新 `npm run pack` |
| Edge 朗读端点是非官方接口 | 所有失败（超时 25s / 403 / 空音频）都静默回退系统语音，用户无感；真失效时把 `tts.engine` 默认改回 `system` 即可 |
| 有道 `jsonapi` 也是非官方接口 | 「查到了」的判据是**解析出字段**而不是 HTTP 状态码——该接口缺请求头时会返回 200 + 空 body；解析失败一律不写缓存、显示失败态。它被当成**一个可插拔的源**，将来可加备用源自动降级。详见 8.3 |
| 生词/释义数据外发 | 只有"被点的那个单词"发给有道，不发整句、不发书名、不发本地路径；缓存是本地的，设置页可一键清除。详见 8.7 |
| 内容版权 | 软件是「播放器」，教材内容全部由使用者自己导入。**内置的那本示例书是自编的 288 句**（按 72 个语法点的线排，从 be 动词一路到被动语态），不含任何出版教材原文，也不随软件分发别人的书。改内容要同时加 `state.rs::DEMO_VERSION`，否则老装机不会重新播种 |

### 7.1 四个窗口实现坑（踩过，别重复踩）

**资料库窗口的不透明度会被 tao 抹掉。** `managerOpacity` 走原生 `WS_EX_LAYERED` + `SetLayeredWindowAttributes`，而 tao 在窗口标志变化时（`show()` 会改 VISIBLE）会用**自己缓存的** ex-style 覆盖 `GWL_EXSTYLE`（`window_state.rs::apply_diff`），刚设的 LAYERED 位就没了，窗口表现为全不透明——但设置页读的是 config，仍显示用户设的值，于是「设置 50%、实际 100%」。而且 tao 那些窗口操作是 `PostMessageW` **异步**投递的，所以在 `show()` 之后直接再设一次也会被后到的覆盖。对策（`windows.rs::show_manager`）：`apply_manager_opacity` 之后再用 `app.run_on_main_thread()` 排一条主线程任务，同队列 FIFO、排在 tao 那条之后，最终生效的一定是我们的 alpha；另外 `set_alpha` 改完扩展位要补 `SetWindowPos(SWP_FRAMECHANGED)`，并在 `WindowEvent::Resized` 上再补一次（最大化 / 还原同样会触发 apply_diff）。

**悬浮窗的透明和不透明度是两套机制，别混。** 窗口透明来自 tao 的 `DwmEnableBlurBehindWindow`（DWM 级，不会被动 ex-style），不透明度来自页面 CSS `#widget.style.opacity`。所以「清掉 LAYERED 位」只会影响资料库窗口，不影响悬浮窗。

**样式表里一条 `app-region` / `-webkit-app-region` 都不能留。** 这是 Electron 时代的写法，`bridge/api.js` 早就改成主动调 `widget_drag` 了，但 `src/widget/index.html` 里那 4 条属性一直留着——直到 wry 0.55 在 `webview2/mod.rs` 里打开了 `SetIsNonClientRegionSupportEnabled(true)`，它就从死代码变成了活跃的破坏源：整窗被 WebView2 当成**非客户区（标题栏）**，鼠标移动事件不再派发给渲染进程，CSS `:hover` 会**卡在最后一次状态**——表现是「悬停显示」的中文一旦因鼠标进入而显示，移开鼠标也永不消失；同一机制的底部控制栏、右下角 resize 手柄同样永久显示。标了 `no-drag` 的子元素（按钮、可编辑标题）被排除在非客户区之外，所以按钮照常能点，极具迷惑性。对策：删掉全部 `-webkit-app-region`，拖动只走 `bridge/api.js` 的 `mousedown → widget_drag`（它用 `closest('#widget')` + 排除列表实现 drag / no-drag 语义）。

**悬浮窗的「隐藏」必须自己调 `ShowWindow`，`win.hide()` 是空操作。** 悬浮窗以 `visible(false)` 创建，之后用原生 `ShowWindow(SW_SHOWNOACTIVATE)` 显示（为了不抢焦点），tao 的 `WindowFlags` 里 `VISIBLE` 始终停在 false —— 而 `apply_diff` 在 `new == old` 时**直接 return**（`window_state.rs:321`）。于是 `win.hide()` 算出的 diff 为空、什么都不做：config 写了「已隐藏」，窗口却还挂在屏幕上。对策（`windows.rs::hide_widget`）：与显示对称，走 `win32::hide`（原生 `ShowWindow(SW_HIDE)`）。

**「隐藏悬浮窗」的状态不要跨会话持久化。** 原来启动时读 `config.widgetVisible`，false 就既不 `show` 也不 `reveal`；于是上次误触一次隐藏（点了底部控制栏的 ✕，或按了 `Alt+H`），下次启动窗口就完全没有踪影 —— 而找回入口只有托盘图标和全局快捷键这两个都不显眼的地方，用户只能得出「程序在跑，但桌面上什么都看不到」。现在 `lib.rs` 启动时无条件把 `widgetVisible` 重置为 true 并落盘：**隐藏只对当前会话有效**。

---

## 8. 点词查义的设计

### 8.1 为什么不是「打包离线词库」

最直觉的方案是打包一份英汉词库本地查表。实测 ECDICT 按词频取 3 万词约 3.4 MB（gzip 1.3 MB），体积其实不吓人。

但**要查的词天然偏生僻** —— `the` / `a` / `of` / `is` 这些高频词用户根本不会去点。所以一份"高频词库"对这个功能几乎没有收益，容量花在刀背上了。结论：**全走在线，查到后缓存到本地**。缓存顺便解决重复点击，也天然是将来做生词本的现成数据。

### 8.2 为什么是有道 jsonapi

可选源逐个试过（都是实际打过请求验证的，不是凭印象）：

| 源 | 中文释义 | 要不要 key | 额度 | 结果 |
|---|---|---|---|---|
| **有道 jsonapi**（非官方） | 有，含**真 IPA + 词性 + 词形还原** | 不要 | 无 | **采用** |
| 有道智云官方 API | 最好 | 要，且不能自助开通（工作日电话/邮件申请） | — | 条款**明文禁止缓存**，与本设计冲突，出局 |
| 百度翻译开放平台 | 有 | 要，需实名 | 个人 100 万字符/月 | 只返回 `trans_result[].dst` 一行释义；音标词性在 `dict` 字段里，而那是**企业尊享版专属**，个人拿不到 |
| `api.dictionaryapi.dev` | **没有，只有英英** | 不要 | 无 | Wiktionary 系，解决不了中文 |
| `zh.wiktionary.org` | 有 | 不要 | — | **实测 http_code=000，维基系整体被墙** |
| Google 非官方免 key 接口 | 最好 | 不要 | — | 大陆不通 |
| MyMemory | 有 | 不要 | 匿名约 1000 词/天 | 质量差：`whisper` → **窃语** |

结构性原因：**中文释义这个数据全球只有有道、百度、必应、谷歌几家有，全是商业资产**。所以「免 key + 合法 + 中文释义」这个组合在结构上就不成立 —— GitHub 上叫得出名字的"免费免 key 词典 API"清一色是 Wiktionary 系，返回的必然是英英。

### 8.3 已知的两个坑（实测，不是推测）

**① 缺请求头会「静默失败」。** 该接口必须同时带浏览器 `User-Agent` 和 `Referer: https://dict.youdao.com/`，**少任何一个都返回 HTTP 200 但 body 为 0 字节**。所以判定「查到了」的依据只能是"解析出完整字段"（`ec.word[0]` + 至少一条带词性的释义），绝不能用状态码 —— 否则接口哪天变了，用户看到的是一片空白卡片，日志里却全是 200。

**② 有道已经加固过一次网页版接口。** 网页版的 `dict.youdao.com/jsonapi_s` 现在必须带 `sign` 签名（md5 三步 + 一个写死在前端 JS 里的固定密钥），移动端这个 `jsonapi` 暂时还没加。这是"逆向接口随时会变"的实证 —— 不是假设风险，是已经发生过一次的事，只是没波及我们用的这个端口。

对策不是赌它不变，而是**把它做成一个可插拔的源**：失败就降级（本地缓存 → 有道 → 备用源 → 卡片兜底按钮）。将来换源只动 `dict.rs` 一个函数。

### 8.4 数据流

```
点单词
  └─ 本地缓存命中？ ─ 是 ─→ 直接出卡（毫秒级）
                     └ 否 ─→ dict.youdao.com/jsonapi
                              ├─ 解析成功 → 裁剪 → 写 data/cache/dict.json → 出卡
                              └─ 失败     → 失败卡（原因 + 重试 / 复制单词）
```

### 8.5 裁剪规则（原响应最大 517 KB，必须裁）

实测同一接口不同词的响应体积差异极大：`tree` 49 KB、`whisper` 60 KB、`book` 122 KB、`light` 295 KB、`set` 391 KB、**`run` 517 KB**。原因是返回体里塞了柯林斯、双语例句、同义词、词根词缀、影视原声等一大堆，多义词爆炸式膨胀。**直接缓存原响应，5000 个词就是 250 MB 级** —— 正是最初想避开的东西。

所以缓存只存裁剪结果（每词约 150~250 字节）：

```json
{ "w": "whisper", "us": "ˈwɪspər", "uk": "ˈwɪspə(r)", "proto": null,
  "trs": [ { "pos": "v.", "means": ["耳语，低语，小声说", "传说（某事），谣传", "发沙沙声…"] },
           { "pos": "n.", "means": ["耳语（声）", "沙沙声", "传闻，谣言"] } ] }
```

- 最多 4 个词性、每个词性最多 3 条子义项、每条最多 30 字（超出加省略号）
- **词性不在任何字段里**，而是释义字符串的前缀（`"v. 耳语，低语…"`），`trs[].pos` 恒为 `null` —— 得从文本里拆
- 没有标准词性前缀的义项（`"【名】 (Sitting) （美、加）西特（人名）"`）直接丢弃，这类对人名地名的解释对阅读没价值
- 释义路径固定是 `ec.word[0].trs[][tr][0].l.i[0]`；顶层没有 `ec` 键就是没收录这个词
- 接口白送的两个有用字段一并留存（暂不显示）：`prototype` 词形还原、`wfs` 词形变化表

### 8.6 交互

- **卡片位置**：排在英文句和中文之间，属于正常文档流。原因是**窗口画不到自己外面** —— 做成绝对定位的浮层会被窗口边界裁掉；放在流里则由已有的 `fitWindow()` 自动撑高窗口，不需要任何手动窗口管理。
- 宽度上限跟着用户的 `widgetMaxWidth` 走（`min(420, 上限)`），免得释义一长就把窗口撑过他设的上限。
- **成功态不显示任何来源和耗时**，只有失败态才把原因摆出来。
- 关闭方式：再点同一个词 / 点句子空白处 / `Esc` / 切到下一句。
- 连点多个词时用请求序号丢弃过期结果，避免先返回的请求把后点的词覆盖掉。
- 卡片上的 🔊 复用已有的神经语音（`tts_speak` 传单个词），命中同一套 `data/cache/tts/` 缓存。

### 8.7 隐私与版权

- 接口在 Rust 侧调用：不受 WebView 跨域与 CSP 限制，将来若加带密钥的备用源，密钥也不进 JS 层。
- 只把"被点的那个单词"发给有道，不发整句、不发书名、不发本地路径。
- `data/cache/dict.json` 是"你查过哪些词"的记录，纯本地，设置页可一键清除。
- 有道 jsonapi 是未公开的产品内部接口，ToS 上属于未授权使用。**个人自用是现实选择，但不适合当作可分发的产品能力** —— 真要分发得换成正式授权的源。
