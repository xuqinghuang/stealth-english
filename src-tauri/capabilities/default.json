{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "两个窗口的默认权限：只需要事件监听，业务命令由应用自己的 invoke_handler 提供",
  "windows": [
    "widget",
    "manager"
  ],
  "permissions": [
    "core:default"
  ]
}
